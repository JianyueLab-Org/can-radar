# can-ui 装在 GitHub Packages 上，而 GitHub 的 npm registry **即使是公开包也要
# 求带令牌** —— 这是 GitHub 的规矩，不是我们的选择，也是这条分发路线唯一的糙
# 点。所以这个文件必须提交：它写的是 registry 地址，不是凭据。
#
# `${GITHUB_TOKEN}` 在安装时才从环境里读，文件本身不含任何密钥。本地是一个带
# `read:packages` 的个人令牌；CI 里是工作流自带的 `secrets.GITHUB_TOKEN`，它本
# 来就有这个权限。Docker 构建见 Dockerfile 里的 secret 挂载。
@jianyuelab-org:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
