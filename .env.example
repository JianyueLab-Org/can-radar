# 航迹查询转发到哪儿。flightPosition 的 schema 归 can-api 所有，这个站点不碰
# 数据库。路径没变，只是主机从 can-web 换成了 can-api。
CAN_API_ORIGIN=https://api.airwaysn.org

# 导航数据**不再属于这个站**。/api/v1/route 现在转发到 can-api，那边读它自己挂
# 的 NAVDATA_DIR。上游没挂导航数据时会回 503，地图照旧退回直飞弧线。
