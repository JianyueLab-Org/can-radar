# 航迹查询转发到哪儿。flightPosition 的 schema 归 can-api 所有，这个站点不碰
# 数据库。路径没变，只是主机从 can-web 换成了 can-api。
#
# 会话也问它（/api/v1/auth/session）：这个站点不验签，只把浏览器带来的 cookie
# 转过去问一次「这是谁」。见 src/server/session.ts。
CAN_API_ORIGIN=https://api.airwaysn.org

# 主站。页眉那排导航（名册、活动、下载、面板）和**唯一的登录入口**都在那边 ——
# 这里是另一个源，写相对路径会打在雷达自己的域名上然后 404。
#
# 本地开发时主站在 :4321，不改这个值的话开发机上点「登录」会跳到线上去。
CAN_WEB_ORIGIN=https://airwaysn.org

# 这个部署自己的对外地址，只用来校验写操作的 Origin（本站唯一的写操作是登出）。
#
# **部署里必须设。** 不设就是下面这个开发机地址，而浏览器发来的 Origin 是线上
# 域名，两者对不上 —— 登出会稳定地 403。它是显式配置而不是从 Host 头推的，因为
# 这个站跑在 TLS 终止的反代后面，从请求头推出来的东西正是反代能影响的东西。
PUBLIC_ORIGIN=http://localhost:4323

# 导航数据**不再属于这个站**。/api/v1/route 现在转发到 can-api，那边读它自己挂
# 的 NAVDATA_DIR。上游没挂导航数据时会回 503，地图照旧退回直飞弧线。
